/**
 * Boo who
 * Return true if a value is classified as a boolean primitive or otherwise
 *
 * Ashen Gunaratne
 * mail@ashenm.ml
 *
 */

function booWho(bool) {
  // What is the new fad diet for ghost developers? The Boolean.
  return (typeof bool === "boolean") ? true : false;
}
