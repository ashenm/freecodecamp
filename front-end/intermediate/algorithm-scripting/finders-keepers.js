/**
 * Finders Keepers
 * Returns the first element in the array that passes a given argument
 *
 * Ashen Gunaratne
 * mail@ashenm.ml
 *
 */

function findElement(arr, func) {
  const num = 0;
  return arr.filter(func)[num];
}
